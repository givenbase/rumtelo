export type FireworksOptions = {
    /** How long bursts keep firing. Default 1800ms. */
    durationMs?: number;
    /** Canvas stacking. Default 80. */
    zIndex?: number;
    colors?: string[];
};
