interface StubNoticeProps {
    what: string;
    /** Prefix before `what` — pass from `ui.statusPage.scaffold`. */
    prefix: string;
}

export type { StubNoticeProps };
export default StubNoticeProps;
