/**
 * Catalogs Types (Growth)
 * Re-exports from schema (same-module merge).
 */

export type {
    IncomePosture,
    LearnBookPreset,
    LearnWatchPreset,
    WealthStage,
    GrowthLeverPreset,
} from './catalogs.schema';
