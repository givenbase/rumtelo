/**
 * Catalogs Types (Money)
 * Re-exports from schema (same-module merge).
 */

export type {
    CategoryTemplate,
    FixedCostPreset,
    DebtPreset,
    IncomeSourcePreset,
    GoalPreset,
    MerchantPreset,
    GivingSignal,
    GivingOrganisation,
} from './catalogs.schema';
