/**
 * Catalogs Types (Money)
 * Re-exports from schema (same-module merge).
 */

export type {
    Audience,
    Bank,
    CategoryTemplate,
    FixedCostPreset,
    DebtPreset,
    JarGuide,
    JarTemplate,
    IncomeSourcePreset,
    TransactionInPreset,
    GoalPreset,
    MerchantPreset,
    GivingSignal,
    GivingOrganisation,
} from './catalogs.schema';
