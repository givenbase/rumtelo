/**
 * Catalogs Types (Money)
 * Re-exports from schema (same-module merge).
 */

export type {
    CategoryTemplate,
    FixedCostPreset,
    DebtPreset,
    JarGuide,
    JarTemplate,
    IncomeSourcePreset,
    TransactionInPreset,
    GoalPreset,
    MerchantPreset,
    MerchantHighlight,
    GivingSignal,
    GivingOrganisation,
} from './catalogs.schema';
