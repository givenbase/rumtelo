/** Re-export shared Nest/oRPC → i18n helpers (implementation in `@rumtelo/utils`). */
export {
    resolveApiUserMessage,
    parseApiUserMessage,
    extractApiErrorPayload,
    isIbanApiErrorMessage,
} from '@rumtelo/utils';
